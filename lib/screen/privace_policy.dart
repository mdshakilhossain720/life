import 'package:flutter/material.dart';
class PrivaceyPolicy extends StatelessWidget {
  const PrivaceyPolicy({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [
          Text('Privcy And Policy'),
        ],
      ),
    );
  }
}
